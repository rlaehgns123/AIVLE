server:
  port: 8080

spring:
  application:
    name: bookapp
  datasource:
    url: jdbc:mysql://localhost:3306/bookapp?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
      hibernate:
      format_sql: true